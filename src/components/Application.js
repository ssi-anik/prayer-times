import React, { Component } from "react";

class Application extends Component {
    render () {
        return (
            <div className = "container-fluid">this is container fluid</div>
        );
    }
}

export default Application;